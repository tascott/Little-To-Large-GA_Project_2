<div class="outerform">

<%= form_for([@event, @subcategory]) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% @subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.select :name, [["Food", "Food"], ["Venue", "Venue"], ["Decorations", "Decorations"], ["Entertainment", "Entertainment"]] %>


  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

<%= f.hidden_field :event_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div>